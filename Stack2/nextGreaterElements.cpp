#include<iostream>
#include<math.h>
#include<string>
#include<stack>
#include<vector>
using namespace std;
vector<int> nge(vector<int> &arr){
    int n=arr.size();
    vector<int> output(n,-1); 
    stack<int> st;    // index
    for(int i=0;i<n;i++){
        
    }
    return output;
}
int main(){
    stack<int> st;
    int a;
    cin>>a;
    while(a!=-1){
        st.push(a);
        cin>>a;
    }
    vector<int> soln=nge()
    return 0;
}